import { StyleSheet } from "react-native";

export const SButtonVisibilidadeSenha = () => {
    return StyleSheet.create({
        icone : {
            width :25,
            height:25,
        }
    });
}