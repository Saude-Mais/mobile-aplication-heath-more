import { StyleSheet } from "react-native";

export const SButtonHomeCriancaMenu = ()=>{
    return StyleSheet.create({
        iconMenu:{
            width :20,
            height:20,
        }
    });
}